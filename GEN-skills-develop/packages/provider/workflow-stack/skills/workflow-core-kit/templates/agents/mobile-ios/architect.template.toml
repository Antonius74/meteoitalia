name = "architect"
description = "mobile-ios overlay for architect role constraints."
model = "gpt-5.4"
model_reasoning_effort = "high"

developer_instructions = """
Start from shared architect role template and apply iOS boundary and dependency rules.
Do not duplicate shared handoff logic.
"""
