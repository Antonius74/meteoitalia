name = "developer"
description = "mobile-android overlay for developer role constraints."
model = "gpt-5.3-codex"
model_reasoning_effort = "high"

developer_instructions = """
Start from shared developer role template and apply Android implementation rules.
Keep fix-loop behavior and completion reporting aligned with shared contract.
"""
